# Documentation Archive

This folder holds retired or phase-specific Markdown files that are still useful for reference but are no longer part of the main active docs set.

Use the files in `../README.md` and the root repo Markdown set first.
