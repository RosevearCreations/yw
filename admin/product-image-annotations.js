function json(data, status = 200) { return new Response(JSON.stringify(data), { status, headers: { "Content-Type": "application/json" } }); }
function normalizeText(value) { return String(value || "").trim(); }
function getDb(env){ return env.DB || env.DD_DB; }
function getBearerToken(request) { const authHeader = request.headers.get("Authorization") || ""; const match = authHeader.match(/^Bearer\s+(.+)$/i); return match ? String(match[1] || "").trim() : ""; }
async function getAdminUserFromRequest(request, env) { const db=getDb(env); const token = getBearerToken(request); if (!db || !token) return null; try{ const session = await db.prepare(`SELECT s.session_id,s.user_id,u.user_id AS resolved_user_id,u.email,u.display_name,u.role,u.is_active FROM sessions s INNER JOIN users u ON u.user_id = s.user_id WHERE (s.session_token = ? OR s.token = ?) AND s.expires_at > datetime('now') LIMIT 1`).bind(token, token).first(); if (!session || Number(session.is_active || 0) !== 1 || String(session.role || '').toLowerCase() !== 'admin') return null; return { user_id: Number(session.resolved_user_id || session.user_id || 0), email: session.email || '', display_name: session.display_name || '', role: 'admin' }; }catch{return null;} }
function normalizeResults(result) { return Array.isArray(result?.results) ? result.results : []; }
export async function onRequestGet(context) { const { request, env } = context; const db=getDb(env); const adminUser = await getAdminUserFromRequest(request, env); if (!adminUser) return json({ ok: false, error: 'Unauthorized.' }, 401); const product_id = Number(new URL(request.url).searchParams.get('product_id')); if (!Number.isInteger(product_id) || product_id <= 0) return json({ ok: false, error: 'A valid product_id is required.' }, 400); try{ const rows = normalizeResults(await db.prepare(`SELECT product_image_annotation_id, product_id, product_image_id, image_url, alt_text, image_title, caption, focal_point_x, focal_point_y, annotation_notes, updated_at FROM product_image_annotations WHERE product_id = ? ORDER BY product_image_annotation_id ASC`).bind(product_id).all()); return json({ ok: true, annotations: rows.map((row) => ({ product_image_annotation_id:Number(row.product_image_annotation_id||0), product_id:Number(row.product_id||0), product_image_id:Number(row.product_image_id||0)||null, image_url:row.image_url||'', alt_text:row.alt_text||'', image_title:row.image_title||'', caption:row.caption||'', focal_point_x:row.focal_point_x ?? null, focal_point_y:row.focal_point_y ?? null, annotation_notes:row.annotation_notes||'', updated_at:row.updated_at||null })) }); }catch{ return json({ ok:true, annotations:[] }); } }
export async function onRequestPost(context) { const { request, env } = context; const db=getDb(env); const adminUser = await getAdminUserFromRequest(request, env); if (!adminUser) return json({ ok: false, error: 'Unauthorized.' }, 401); let body = {}; try { body = await request.json(); } catch { return json({ ok: false, error: 'Invalid JSON body.' }, 400); } const product_id = Number(body.product_id); if (!Number.isInteger(product_id) || product_id <= 0) return json({ ok: false, error: 'A valid product_id is required.' }, 400); const annotations = Array.isArray(body.annotations) ? body.annotations : []; try{ await db.prepare(`DELETE FROM product_image_annotations WHERE product_id = ?`).bind(product_id).run(); for (const row of annotations.slice(0, 20)) { await db.prepare(`INSERT INTO product_image_annotations (product_id, product_image_id, image_url, alt_text, image_title, caption, focal_point_x, focal_point_y, annotation_notes, updated_at) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, CURRENT_TIMESTAMP)`).bind(product_id, Number(row.product_image_id) || null, normalizeText(row.image_url) || null, normalizeText(row.alt_text) || null, normalizeText(row.image_title) || null, normalizeText(row.caption) || null, row.focal_point_x == null ? null : Number(row.focal_point_x), row.focal_point_y == null ? null : Number(row.focal_point_y), normalizeText(row.annotation_notes) || null).run(); } return json({ ok: true, message: 'Image annotations saved.' }); }catch(e){ return json({ ok:false, error:e.message || 'Unable to save annotations.' },500); } }
