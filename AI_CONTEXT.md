# AI Context

Current build: **2026-06-04b**  
Current schema: **130**

Continue from the schema 130 build. The repaired schema 128 roadmap-column mismatch is preserved in both the standalone migration and the canonical full schema. Schema 130 adds DB-visible execution queues for payment application, bank reconciliation, equipment scan/template work, local SEO publication, and fallback drills.

Always keep active Markdown, SQL migrations, `sql/000_full_schema_reference.sql`, cache markers, Admin readiness views, CSS/H1 checks, and smoke checks synchronized in each pass.
